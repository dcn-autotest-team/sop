# 📁 AGENTS.MD / 项目能力沉淀 SOP 标准模板

> 本文件是项目完成后的“数字化资产说明书”。它既是**给后续接手的 AI 看的系统级约束**，也是**给人类工程师看的标准作业程序（SOP）**。

---

## 1. 项目定位与核心上下文 (Context & Architecture)
- **业务场景/目标**：解决什么核心问题？
- **技术选型与版本**：
  - 运行时/语言：
  - 核心框架与版本：
  - 外部依赖与第三方服务：
- **目录结构与模块边界**：
  ```
  src/
  ├── core/       # 核心业务逻辑（禁止依赖外部接口）
  ├── adapters/   # 外部服务适配器
  └── utils/      # 纯函数通用工具
  ```

---

## 2. 从 0 到 1 标准执行路径 (Step-by-Step SOP)
> 遇到类似需求时，严格按照以下顺序推进，避免走弯路：

1. **第 1 阶段：环境与基础骨架准备**
   - 初始化命令 / 脚手架
   - 必需的环境变量与配置文件
2. **第 2 阶段：核心领域模型与数据流设计**
   - 数据结构定义
   - 接口契约规范
3. **第 3 阶段：关键链路实现与分步验证**
   - 核心功能实现
   - 单点冒烟测试
4. **第 4 阶段：边缘场景处理与上线检查**
   - 错误兜底与降级逻辑
   - 构建打包与部署命令

---

## 3. 避坑指南与反工自查清单 (Pitfalls & Checklist)
> 记录本次项目中发生返工、踩坑、调试耗时过长的教训：

| 陷阱/报错现象 | 根本原因 | 标准解决/避坑姿势 |
| :--- | :--- | :--- |
| *例：接口跨域/鉴权失败* | *CORS头缺失或Cookie未携带* | *统一在 Gateway 层配置，并在客户端开启 withCredentials* |
| *例：异步竞态导致状态错乱* | *未做取消令牌或防抖* | *统一使用 AbortController* |

### 启动前 / 提交前 Checklist
- [ ] 环境变量是否都有默认值或缺失报错？
- [ ] 关键异常是否有重试或明确提示？
- [ ] 关键代码是否保持纯函数、解耦良好？

---

## 4. 最佳实现与代码模式 (Best Practices & Patterns)
> 提供经过验证的、可直接复制的黄金代码片段（Golden Code Snippets）：

### 4.1 核心范式 1：[如：统一错误处理/API包装]
```typescript
// 在此贴入经过实战检验的最优写法
```

### 4.2 核心范式 2：[如：状态流转/复杂数据处理]
```typescript
// 规范示例
```

---

## 5. 面向后续 AI 的系统铁律 (Agent Instructions)
> **把这几条直接写进给 AI 的提示词或 Rules 中**：

- 🚫 **绝对禁止 (Never do)**：
  1. 绝对不要...
  2. 绝对不要...
- ✅ **必须遵循 (Always do)**：
  1. 必须始终...
  2. 必须始终...

---

## 6. 📜 修撰履历与演进时间线 (Timeline)
- **YYYY-MM-DD HH:mm** (`贡献人 & AI Agent`): `[初版归档]` 项目 0-1 标准实践初次沉淀入库。
- **YYYY-MM-DD HH:mm** (`贡献人 & AI Agent`): `[增量升级]` 增补新避坑点与边界处理范式。
